doctype html
html
  head
    title Join the #{community} community on Slack!
    style.
      body {
        padding: 50px;
        font: 14px "Comic Sans MS", sans-serif;
        background-color: yellow;
      }
      input{
        -webkit-appearance: none;
        -moz-appearance: none;
        appearance: none;
      }
      input[type=text]{
        width: 200px;
        border: 1px solid black;
        padding: 2px;
        margin: 2px;
        font: 14px "Comic Sans MS", sans-serif;
      }
      input[type=submit]{
        font-family: "Comic Sans MS", sans-serif !important;
        border: 1px solid black;
        height: 25px;
        margin: 2px;
        background-color: red;
      }
  body
    h1 Join the #{community} community on Slack!
    p Note that your email address will be visible to everyone else on the team. Consider using a throwaway email or 
      a(href="https://mailinator.com/") Mailinator
    form(method="POST", action="/invite")#join-form
      input(type="text", name="email", placeholder="Enter your email")#slack-email
      input(type="submit", value="wow")
    img(src="/assets/computer-doge.jpg")
    script.
      var form = document.getElementById('join-form');
      var email = document.getElementById('slack-email')
      form.addEventListener('submit', function(evt) {
        if (!email.value) {
          evt.preventDefault();
        }
      });
    script.
      (function(i,s,o,g,r,a,m){i['GoogleAnalyticsObject']=r;i[r]=i[r]||function(){
      (i[r].q=i[r].q||[]).push(arguments)},i[r].l=1*new Date();a=s.createElement(o),
      m=s.getElementsByTagName(o)[0];a.async=1;a.src=g;m.parentNode.insertBefore(a,m)
      })(window,document,'script','//www.google-analytics.com/analytics.js','ga');

      ga('create', 'UA-63828172-4', 'auto');
      ga('send', 'pageview');
